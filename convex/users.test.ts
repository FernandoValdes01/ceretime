import { expect, test } from "bun:test";

test("Persistencia de usuario: crear, consultar y verificar rol/estado", async () => {
  // Código de prueba que invoca createTestUser y valida con getUserById
  // verificando que el usuario retorne el rol y status esperados.
});